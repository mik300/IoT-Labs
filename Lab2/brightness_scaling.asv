function  mod_rgb = brightness_scaling(img_rgb, b)
pre_mod_rgb = rgb2hsv(original_img)
mod_rgb(:, :, 1) = img_rgb(:, :, 1) + b;
mod_rgb(:, :, 2) = img_rgb(:, :, 2) + b;
mod_rgb(:, :, 3) = img_rgb(:, :, 3) + b;
end

pre_mod_img = rgb2hsv(original_img);
pre_mod_img(:, :, 3) = pre_mod_img(:, :, 3) * b;
mod_img = hsv2rgb(pre_mod_img);
mod_img = mod_img * 255;
mod_img = cast(mod_img, "uint8");